1. instalar mysql: https://www.youtube.com/watch?v=kfdGLKNBCnY
user: root
admin root
Para entrar a mysql desde cmd: mysql -u root -p
Correr esto para tener un usuario y contraseña que no se expira nunca ->
ALTER USER `root`@`localhost` IDENTIFIED BY 'root',
       `root`@`localhost` PASSWORD EXPIRE NEVER;
2. Entrar en la carpeta api y luego routes, mirar los request examples 
para usar postman.

fin.